{
  "name": "ucd-schedule-export",
  "version": "1.0.0",
  "description": "Exports your current saved schedule on myucdavis schedule builder into an `.ics` file.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
